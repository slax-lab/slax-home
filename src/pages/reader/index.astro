---
import Button from '~/components/Button.astro';
import Footer from '~/components/Footer.astro';
import Nav from '~/components/Nav.astro';
import Base from '~/layouts/Base.astro';

// Content sourced verbatim from original apps/reader-home components
// (archive/apps-backup): HomeHero / HomeFeatures / HomeHowItWorks /
// HomeComparison / HomeCTA / PricingTable / DownloadPage.
// Plan decision: merge features + pricing + download into single
// anchor-navigable /reader/ page.

const features = [
	{
		title: 'AI-Powered Understanding',
		body: "Get instant summaries, key insights, and answers to your questions about any article. It's like having a personal tutor for everything you read.",
	},
	{
		title: 'Content Never Dies',
		body: 'Articles saved once are backed up forever. No more "404 Page Not Found" disappointments. Your digital library is truly permanent.',
	},
	{
		title: 'Smart Organization',
		body: 'AI automatically tags and categorizes your content. Find exactly what you need when you need it with powerful semantic search.',
	},
	{
		title: 'Connected Reading',
		body: 'Highlight and comment on any article you save. Share your content and invite others to add insights and discuss. Every article becomes a place for conversation.',
	},
];

const steps = [
	{
		title: 'Save Anything',
		body: 'One click to save articles, papers, or blog posts from anywhere on the web.',
	},
	{
		title: 'AI Analyzes',
		body: 'Instant AI overviews help you decide what deserves deep reading and what to skim. Save time for the best content.',
	},
	{
		title: 'Ask Questions',
		body: 'Chat with AI about your saved content to deepen understanding.',
	},
	{
		title: 'Engage with Your Content',
		body: 'Highlight, comment & discuss with others. What you write is more valuable than what you read.',
	},
];

// Cell value: 'ok' → ● accent,   'no' → — muted,   any other string → inline text (muted italic)
const compareRows: Array<[string, string, string, string]> = [
	['Unlimited free bookmarks (for early users)', 'ok', 'ok', 'Paid'],
	['Permanent content backup', 'ok', 'Paid', 'Paid'],
	['AI outline', 'Paid', 'ok', 'Paid'],
	['Chat with AI while reading', 'Paid', 'no', 'Paid'],
	[
		'Highlight & comment on original pages & snapshots, two-way synced',
		'ok',
		'no',
		'Paid',
	],
	['Join reader discussions', 'ok', 'no', 'Shared only'],
	['AI auto-tagging', 'Paid', 'Manual', 'Manual'],
];

const matrixRows: Array<[string, string, string]> = [
	['Save Unlimited Articles', 'ok', 'ok'],
	['Permanent Snapshots (no broken links)', 'ok', 'ok'],
	['Unlimited Highlights & Comments', 'ok', 'ok'],
	['Full-Text Search', 'ok', 'ok'],
	['Sync Across All Devices', 'ok', 'ok'],
	['Share Articles & Join Discussions', 'ok', 'ok'],
	['Open Source & Privacy-First', 'ok', 'ok'],
	['Create & Manage Tags', 'ok', 'ok'],
	['Smart AI Auto-Tagging', 'no', 'ai'],
	['Instant AI Summaries', 'no', 'ai'],
	['Interactive AI Outlines', 'no', 'ai'],
	['AI Chatbot — Ask Your Library Anything', 'no', 'ai'],
];

const faqs = [
	{
		q: 'Is the Free plan really free forever?',
		a: "Yes — for early users. We're offering unlimited bookmarks and permanent backups at no cost while we're in early access. Lock in free usage now before limits apply.",
	},
	{
		q: 'Will Slax Reader really save me hours of reading every week?',
		a: "That's what it's built for. Clickable AI outlines let you navigate long articles without reading every word. Auto-tagging handles the organizing, so you can just read.",
	},
	{
		q: 'Can I cancel my Pro subscription anytime?',
		a: 'Absolutely. Cancel any time from your account settings with no penalties. Your saved articles and highlights remain accessible on the Free plan.',
	},
	{
		q: 'How does the 1 Month Free trial work?',
		a: "Sign in and start your trial — no credit card required. After 30 days you'll be asked if you'd like to continue with Pro at $5.99 / month (regular price $9.99).",
	},
];

const platforms = [
	{
		icon: '📱',
		title: 'iPhone, iPad, Android',
		bullets: [
			'Save forever, for free.',
			'Save any article with one tap.',
			'Get instant AI outlines and quick navigation.',
			'Cross-platform sync.',
		],
		downloads: [
			{
				href: 'https://apps.apple.com/cn/app/slax-reader-ai-read-it-later/id6596730998',
				small: 'Download on the',
				main: 'App Store',
			},
			{
				href: 'https://play.google.com/store/apps/details?id=com.slax.reader',
				small: 'GET IT ON',
				main: 'Google Play',
			},
		],
	},
	{
		icon: '💻',
		title: 'Mac',
		bullets: ['Save, read and organize like a pro.'],
		downloads: [
			{
				href: 'https://apps.apple.com/cn/app/slax-reader-ai-read-it-later/id6596730998',
				small: 'Download on the',
				main: 'Mac App Store',
			},
		],
	},
	{
		icon: '🌐',
		title: 'Browser Extension',
		bullets: [
			'One tap to save and back up any web page — yours to keep forever.',
			'AI chat with your articles — right on the same page.',
			'Find what you need fast with powerful semantic search.',
		],
		downloads: [
			{
				href: 'https://chromewebstore.google.com/detail/gdnhaajlomjkhahnmiijphnodkcfikfd?utm_source=web_homepage',
				small: '',
				main: 'Chrome Extension',
			},
		],
	},
];

const readerTrust = [
	'🔒 Your content, forever safe',
	'🚀 5× faster understanding',
	'⭐ Interactive reading community',
];

const schema = {
	'@context': 'https://schema.org',
	'@type': 'SoftwareApplication',
	name: 'Slax Reader',
	applicationCategory: 'ProductivityApplication',
	operatingSystem: 'Web, iOS, iPadOS, Android, macOS',
	description:
		'Save Forever, Learn faster. Your intelligent library for high efficiency and deep understanding.',
	offers: [
		{ '@type': 'Offer', name: 'Free', price: '0', priceCurrency: 'USD' },
		{
			'@type': 'Offer',
			name: 'Pro Reader',
			price: '5.99',
			priceCurrency: 'USD',
		},
	],
	publisher: {
		'@type': 'Organization',
		name: 'Slax Lab',
		url: 'https://slax.com',
	},
};
---
<Base
	title="Slax Reader — Read Smarter, Not Just Later"
	description="Save Forever, Learn faster. Your intelligent library for high efficiency and deep understanding."
	schema={schema}
>
	<Nav active="reader" />

	<main>
		<!-- HERO -->
		<section class="r-hero">
			<div class="r-hero-inner">
				<div>
					<h1 class="r-h1">Read Smarter<br />Not Just Later</h1>
					<p class="r-dek">
						Save Forever, Learn faster. Your intelligent library for high
						efficiency and deep understanding.
					</p>
					<div class="actions">
						<Button variant="primary" href="https://r.slax.com/login" arrow>
							Start Reading Smarter — Free
						</Button>
						<Button variant="secondary" href="#download">Download apps</Button>
					</div>
					<div class="r-trust">
						{readerTrust.map((t) => <span>{t}</span>)}
					</div>
				</div>
				<div class="r-preview" aria-hidden="true">
					<div class="r-preview-card">
						<h4>🤖 AI Analysis</h4>
						<p>
							This article explains how knowledge workers can build a
							permanent reading library. Key points:<br />1. Save content before
							it disappears.<br />2. Use AI to summarize and surface key ideas.
							<br />3. Turn reading into reusable knowledge.
						</p>
					</div>
					<div class="r-preview-card">
						<h4>💬 Ask AI</h4>
						<p>
							<strong>You:</strong> "What should I remember from this
							article?"<br />
							<strong>AI:</strong> "Focus on the permanent archive, AI-assisted
							understanding, and collaboration layer. Those are the main reasons
							users switch."
						</p>
					</div>
				</div>
			</div>
		</section>

		<!-- FEATURES -->
		<section class="features" id="features">
			<div class="features-inner">
				<div class="features-head">
					<h2 class="features-title">Not Just Another Read-Later Tool</h2>
					<p class="features-sub">
						We're building the future of intelligent reading.
					</p>
				</div>
				<div class="feat-grid">
					{features.map((f, i) => (
						<div class="feat">
							<div class="feat-num">{String(i + 1).padStart(2, '0')}</div>
							<h3 class="feat-h">{f.title}</h3>
							<p class="feat-p">{f.body}</p>
						</div>
					))}
				</div>
			</div>
		</section>

		<!-- HOW IT WORKS -->
		<section class="how">
			<div class="how-inner">
				<h2 class="how-head">From Bookmark to Breakthrough.</h2>
				<p class="how-sub">
					Four simple steps to transform how you read and learn.
				</p>
				<div class="how-steps">
					{steps.map((s, i) => (
						<div class="how-step">
							<div class="how-step-num">{String(i + 1).padStart(2, '0')}</div>
							<h3 class="how-step-h">{s.title}</h3>
							<p class="how-step-body">{s.body}</p>
						</div>
					))}
				</div>
			</div>
		</section>

		<!-- COMPARE -->
		<section class="compare">
			<div class="compare-inner">
				<h2 class="compare-head">Why Readers Are Switching to Slax</h2>
				<p class="compare-sub">
					See how we compare to traditional read-later tools.
				</p>
				<div class="compare-table">
					<div class="compare-row head">
						<div>Feature</div>
						<div class="highlight">Slax Reader</div>
						<div>Instapaper</div>
						<div>Readwise</div>
					</div>
					{compareRows.map((row) => (
						<div class="compare-row">
							<div>{row[0]}</div>
							{[1, 2, 3].map((i) => {
								const v = row[i];
								const isHighlight = i === 1;
								let cls = 'cell';
								let content: string = v;
								if (v === 'ok') {
									cls += ' cell-ok';
									content = '●';
								} else if (v === 'no') {
									cls += ' cell-no';
									content = '—';
								} else {
									cls += ' cell-text';
								}
								if (isHighlight) cls += ' highlight';
								return <div class={cls}>{content}</div>;
							})}
						</div>
					))}
				</div>
				<p class="compare-more">
					<a href="/reader/alternatives/">
						Full comparisons — Readwise, Omnivore, Cubox & more →
					</a>
				</p>
			</div>
		</section>

		<!-- PRICING -->
		<section class="pricing" id="pricing">
			<div class="pricing-inner">
				<h2 class="pricing-head">Read Smarter, <em>Not Just Later.</em></h2>
				<p class="pricing-dek">
					Save Forever, Learn faster. Your intelligent library for high
					efficiency and deep understanding.
				</p>
				<a class="pricing-promo" href="https://r.slax.com/login">
					🎉 Join today and get 1 Month of Pro Free
				</a>

				<div class="plans-grid">
					<div class="plan">
						<div class="plan-eyebrow">Free</div>
						<div class="plan-price">
							<span class="num">$0</span><span class="unit">/ month</span>
						</div>
						<p class="plan-tag">
							Unlimited bookmarks & backup forever — join now before limits
							apply!
						</p>
						<a href="https://r.slax.com/login" class="btn-primary">Start Free</a>
					</div>
					<div class="plan featured">
						<div class="plan-eyebrow">Pro Reader</div>
						<div class="plan-price">
							<span class="num">$5.99</span><span class="unit">/ month</span>
							<span class="was">$9.99</span>
						</div>
						<p class="plan-tag">
							Unleash your reading superpowers with integrated AI analysis.
						</p>
						<a href="https://r.slax.com/login" class="btn-primary">Try 1 Month Free</a>
					</div>
				</div>

				<div class="matrix" aria-label="Feature matrix">
					<div class="matrix-row head">
						<div class="cell-feat">Feature</div>
						<div class="cell">Free</div>
						<div class="cell">Pro Reader</div>
					</div>
					{matrixRows.map((row) => (
						<div class="matrix-row">
							<div class="cell-feat">{row[0]}</div>
							{[1, 2].map((i) => {
								const v = row[i];
								if (v === 'ok') return <div class="cell ok">✓</div>;
								if (v === 'ai') return <div class="cell ai">AI</div>;
								return <div class="cell no">✕</div>;
							})}
						</div>
					))}
				</div>
			</div>
		</section>

		<!-- FAQ -->
		<section class="faq">
			<div class="faq-inner">
				<h2 class="faq-head">Frequently Asked Questions</h2>
				{faqs.map((item) => (
					<details class="faq-item">
						<summary class="faq-q">{item.q}</summary>
						<p class="faq-a">{item.a}</p>
					</details>
				))}
			</div>
		</section>

		<!-- DOWNLOAD -->
		<section class="download" id="download">
			<div class="download-inner">
				<h2 class="download-head">
					Slax Reader — <em>Save Forever. Read Smarter.</em>
				</h2>
				<p class="download-dek">
					Free to download, available on all your favorite platforms.
				</p>
				<div class="platforms">
					{platforms.map((p) => (
						<div class="platform">
							<div class="platform-left">
								<div class="platform-head">
									<span class="platform-icon">{p.icon}</span>
									<h3 class="platform-title">{p.title}</h3>
								</div>
								<ul class="platform-list">
									{p.bullets.map((b) => <li>{b}</li>)}
								</ul>
							</div>
							<div class="platform-right">
								{p.downloads.map((d) => (
									<a href={d.href} class="dl-btn" target="_blank" rel="noopener noreferrer">
										{d.small && <span class="dl-btn-small">{d.small}</span>}
										<span class="dl-btn-main">{d.main}</span>
									</a>
								))}
							</div>
						</div>
					))}
				</div>
			</div>
		</section>

		<!-- FINALE -->
		<section class="finale">
			<div class="finale-inner">
				<h2 class="finale-h">
					Start Your <em>Intelligent</em> Reading Journey Today
				</h2>
				<p class="finale-p">Read smarter, learn faster, remember more.</p>
				<div class="actions actions-center">
					<Button variant="primary" href="https://r.slax.com/login" arrow>
						Get Started — It's Free
					</Button>
					<Button variant="secondary" href="https://github.com/slax-lab/slax-reader">
						Read the code
					</Button>
				</div>
			</div>
		</section>
	</main>

	<Footer />
</Base>

<style>
	/* HERO */
	.r-hero {
		padding: 6rem 0 5rem;
	}
	.r-hero-inner {
		max-width: 1180px;
		margin: 0 auto;
		padding: 0 2rem;
		display: grid;
		grid-template-columns: 1fr;
		gap: 4rem;
		align-items: center;
	}
	@media (min-width: 960px) {
		.r-hero-inner {
			grid-template-columns: 1.1fr 1fr;
		}
	}
	.r-h1 {
		font-family: var(--serif);
		font-weight: 400;
		font-size: clamp(2.4rem, 5vw, 4.4rem);
		line-height: 1.05;
		letter-spacing: -0.025em;
		margin: 2rem 0 1.5rem;
		color: var(--ink);
	}
	.r-dek {
		font-family: var(--serif);
		font-size: 1.2rem;
		line-height: 1.55;
		color: var(--ink-soft);
		max-width: 40ch;
		margin: 0 0 2rem;
	}
	.actions {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		align-items: center;
	}
	.actions-center {
		justify-content: center;
	}
	.r-trust {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem 1.5rem;
		margin-top: 2rem;
		color: var(--ink-mute);
		font-family: var(--mono);
		font-size: 0.82rem;
	}
	.r-preview {
		padding: 1.75rem;
		border: 1px solid var(--rule);
		background: var(--bg-deep);
		box-shadow: 0 6px 32px -16px rgba(27, 26, 23, 0.18);
	}
	.r-preview-card {
		padding: 1.1rem 1.25rem;
		background: var(--bg);
		border: 1px solid var(--rule-soft);
	}
	.r-preview-card + .r-preview-card {
		margin-top: 0.9rem;
	}
	.r-preview-card h4 {
		font-family: var(--mono);
		font-size: 0.78rem;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		color: var(--accent);
		margin: 0 0 0.6rem;
	}
	.r-preview-card p {
		font-family: var(--serif);
		font-size: 0.95rem;
		line-height: 1.55;
		color: var(--ink-soft);
		margin: 0;
	}
	.r-preview-card :global(strong) {
		font-weight: 500;
		color: var(--ink);
	}

	/* FEATURES */
	.features {
		padding: var(--section-pad) 0;
		border-top: 1px solid var(--rule-soft);
	}
	.features-inner {
		max-width: 1180px;
		margin: 0 auto;
		padding: 0 2rem;
	}
	.features-head {
		margin-bottom: 4rem;
	}
	.features-title {
		font-family: var(--serif);
		font-weight: 400;
		font-size: clamp(1.8rem, 3vw, 2.4rem);
		line-height: 1.15;
		letter-spacing: -0.02em;
		margin: 1rem 0 0.5rem;
	}
	.features-sub {
		font-family: var(--serif);
		font-size: 1.1rem;
		color: var(--ink-soft);
		margin: 0;
		max-width: 44ch;
	}
	.feat-grid {
		display: grid;
		grid-template-columns: 1fr;
		gap: 0;
		border-top: 1px solid var(--rule);
	}
	@media (min-width: 760px) {
		.feat-grid {
			grid-template-columns: 1fr 1fr;
		}
	}
	@media (min-width: 1040px) {
		.feat-grid {
			grid-template-columns: repeat(4, 1fr);
		}
	}
	.feat {
		padding: 2.5rem 2rem;
		border-bottom: 1px solid var(--rule);
	}
	@media (min-width: 760px) {
		.feat:nth-child(2n) {
			border-left: 1px solid var(--rule);
		}
	}
	@media (min-width: 1040px) {
		.feat:nth-child(2n) {
			border-left: 0;
		}
		.feat:not(:nth-child(4n + 1)) {
			border-left: 1px solid var(--rule);
		}
	}
	.feat-num {
		font-family: var(--mono);
		font-size: 0.7rem;
		letter-spacing: 0.2em;
		color: var(--ink-mute);
		margin-bottom: 1.5rem;
	}
	.feat-h {
		font-family: var(--serif);
		font-weight: 500;
		font-size: 1.2rem;
		letter-spacing: -0.015em;
		margin: 0 0 0.75rem;
		line-height: 1.25;
	}
	.feat-p {
		font-family: var(--serif);
		font-size: 0.98rem;
		line-height: 1.55;
		color: var(--ink-soft);
		margin: 0;
		max-width: 38ch;
	}

	/* HOW */
	.how {
		padding: var(--section-pad) 0;
		border-top: 1px solid var(--rule-soft);
		background: var(--bg-deep);
	}
	.how-inner {
		max-width: 1180px;
		margin: 0 auto;
		padding: 0 2rem;
	}
	.how-head {
		font-family: var(--serif);
		font-weight: 400;
		font-size: clamp(1.8rem, 3vw, 2.4rem);
		line-height: 1.15;
		letter-spacing: -0.02em;
		margin: 1rem 0 0.5rem;
	}
	.how-sub {
		font-family: var(--serif);
		font-size: 1.1rem;
		color: var(--ink-soft);
		margin: 0 0 4rem;
	}
	.how-steps {
		display: grid;
		grid-template-columns: 1fr;
		gap: 2.5rem;
	}
	@media (min-width: 820px) {
		.how-steps {
			grid-template-columns: repeat(4, 1fr);
		}
	}
	.how-step {
		text-align: left;
		padding-top: 1.5rem;
		border-top: 1px solid var(--rule);
	}
	.how-step-num {
		font-family: var(--serif);
		font-weight: 300;
		font-size: 2.75rem;
		line-height: 1;
		letter-spacing: -0.03em;
		color: var(--accent);
		margin: 0 0 1.5rem;
	}
	.how-step-h {
		font-family: var(--serif);
		font-weight: 500;
		font-size: 1.3rem;
		letter-spacing: -0.015em;
		line-height: 1.2;
		margin: 0 0 0.6rem;
	}
	.how-step-body {
		font-family: var(--serif);
		font-size: 1rem;
		line-height: 1.55;
		color: var(--ink-soft);
		margin: 0;
		max-width: 32ch;
	}

	/* COMPARE */
	.compare {
		padding: var(--section-pad) 0;
		border-top: 1px solid var(--rule-soft);
	}
	.compare-inner {
		max-width: 1180px;
		margin: 0 auto;
		padding: 0 2rem;
	}
	.compare-head {
		font-family: var(--serif);
		font-weight: 400;
		font-size: clamp(1.8rem, 3vw, 2.4rem);
		line-height: 1.15;
		letter-spacing: -0.02em;
		margin: 1rem 0 0.5rem;
	}
	.compare-sub {
		font-family: var(--serif);
		font-size: 1.1rem;
		color: var(--ink-soft);
		margin: 0 0 3rem;
	}
	.compare-table {
		border-top: 1px solid var(--rule);
		font-family: var(--serif);
		font-size: 1rem;
		overflow-x: auto;
	}
	.compare-row {
		display: grid;
		grid-template-columns: 1.5fr 1fr 1fr 1fr;
		border-bottom: 1px solid var(--rule);
		min-width: 640px;
	}
	.compare-row > div {
		padding: 1.1rem 1rem;
		color: var(--ink-soft);
	}
	.compare-row > div:first-child {
		color: var(--ink);
	}
	.compare-row > div.highlight {
		color: var(--accent);
		background: color-mix(in srgb, var(--accent) 5%, transparent);
	}
	.compare-row.head > div {
		font-family: var(--mono);
		font-size: 0.72rem;
		letter-spacing: 0.18em;
		text-transform: uppercase;
		color: var(--ink-mute);
		padding: 0.8rem 1rem;
	}
	.compare-row.head > div:not(:first-child) {
		text-align: center;
	}
	.compare-row.head > div.highlight {
		color: var(--accent);
		font-weight: 500;
	}
	.compare-row .cell {
		text-align: center;
	}
	.compare-row .cell-ok {
		color: var(--accent);
		font-size: 1.05rem;
	}
	.compare-row .cell-no {
		color: var(--ink-mute);
		font-size: 1.2rem;
	}
	.compare-row .cell-text {
		color: var(--ink-mute);
		font-family: var(--mono);
		font-size: 0.8rem;
		letter-spacing: 0.04em;
	}
	.compare-row .cell-ok.highlight {
		background: color-mix(in srgb, var(--accent) 5%, transparent);
	}
	.compare-more {
		margin: 2rem 0 0;
		font-family: var(--mono);
		font-size: 0.85rem;
	}
	.compare-more a {
		color: var(--ink);
		text-decoration: none;
		border-bottom: 1px dotted var(--ink-mute);
		transition:
			color 0.2s,
			border-color 0.2s;
	}
	.compare-more a:hover {
		color: var(--accent);
		border-color: var(--accent);
	}

	/* PRICING */
	.pricing {
		padding: var(--section-pad) 0;
		border-top: 1px solid var(--rule-soft);
		background: var(--bg-deep);
		scroll-margin-top: 5rem;
	}
	.pricing-inner {
		max-width: 1180px;
		margin: 0 auto;
		padding: 0 2rem;
	}
	.pricing-head {
		font-family: var(--serif);
		font-weight: 400;
		font-size: clamp(2rem, 3.5vw, 3rem);
		line-height: 1.1;
		letter-spacing: -0.025em;
		margin: 1rem 0 0.5rem;
	}
	.pricing-head :global(em) {
		font-style: italic;
		color: var(--accent);
		font-weight: 300;
	}
	.pricing-dek {
		font-family: var(--serif);
		font-size: 1.15rem;
		line-height: 1.5;
		color: var(--ink-soft);
		max-width: 48ch;
		margin: 0 0 1.5rem;
	}
	.pricing-promo {
		display: inline-block;
		font-family: var(--mono);
		font-size: 0.82rem;
		letter-spacing: 0.04em;
		padding: 0.5rem 1rem;
		border: 1px solid var(--accent);
		border-radius: 999px;
		color: var(--accent);
		text-decoration: none;
		margin-bottom: 3rem;
	}
	.pricing-promo:hover {
		background: var(--accent);
		color: var(--bg);
	}
	.plans-grid {
		display: grid;
		grid-template-columns: 1fr;
		gap: 0;
		border-top: 1px solid var(--rule);
		border-bottom: 1px solid var(--rule);
	}
	@media (min-width: 820px) {
		.plans-grid {
			grid-template-columns: 1fr 1fr;
		}
	}
	.plan {
		padding: 2.5rem 2.25rem;
		background: var(--bg);
		display: flex;
		flex-direction: column;
		align-items: flex-start;
	}
	@media (min-width: 820px) {
		.plan + .plan {
			border-left: 1px solid var(--rule);
		}
	}
	@media (max-width: 819px) {
		.plan + .plan {
			border-top: 1px solid var(--rule);
		}
	}
	.plan.featured {
		background: var(--bg-deeper);
		position: relative;
	}
	.plan.featured::before {
		content: "Recommended";
		position: absolute;
		top: 1rem;
		right: 1rem;
		font-family: var(--mono);
		font-size: 0.68rem;
		letter-spacing: 0.15em;
		text-transform: uppercase;
		color: var(--accent);
	}
	.plan-eyebrow {
		font-family: var(--serif);
		font-size: 1.5rem;
		font-weight: 500;
		color: var(--ink);
	}
	.plan.featured .plan-eyebrow {
		color: var(--accent);
	}
	.plan-price {
		display: flex;
		align-items: baseline;
		gap: 0.5rem;
		margin: 0.5rem 0 1rem;
	}
	.plan-price .num {
		font-family: var(--serif);
		font-size: 2.75rem;
		font-weight: 400;
		color: var(--ink);
		letter-spacing: -0.03em;
	}
	.plan-price .unit {
		font-family: var(--sans);
		font-size: 0.92rem;
		color: var(--ink-mute);
	}
	.plan-price .was {
		font-family: var(--mono);
		font-size: 0.85rem;
		color: var(--ink-mute);
		text-decoration: line-through;
		margin-left: 0.4rem;
	}
	.plan-tag {
		font-family: var(--serif);
		font-style: italic;
		font-size: 1rem;
		color: var(--ink-soft);
		margin: 0 0 1.5rem;
		max-width: 38ch;
		line-height: 1.45;
	}

	.matrix {
		margin-top: 3rem;
		font-family: var(--serif);
		font-size: 1rem;
	}
	.matrix-row {
		display: grid;
		grid-template-columns: 1.6fr 0.6fr 0.6fr;
		padding: 0.9rem 1rem;
		border-bottom: 1px solid var(--rule);
		align-items: baseline;
	}
	.matrix-row.head {
		font-family: var(--mono);
		font-size: 0.72rem;
		letter-spacing: 0.18em;
		text-transform: uppercase;
		color: var(--ink-mute);
		border-bottom: 1px solid var(--ink);
		padding: 0.6rem 1rem;
	}
	.matrix-row .cell-feat {
		color: var(--ink);
	}
	.matrix-row .cell {
		text-align: center;
		color: var(--ink-soft);
	}
	.matrix-row .cell.ok {
		color: var(--accent);
	}
	.matrix-row .cell.ai {
		color: var(--accent);
		font-family: var(--mono);
		font-size: 0.78rem;
		letter-spacing: 0.05em;
	}
	.matrix-row .cell.no {
		color: var(--ink-mute);
	}

	/* FAQ */
	.faq {
		padding: var(--section-pad) 0;
		border-top: 1px solid var(--rule-soft);
	}
	.faq-inner {
		max-width: 820px;
		margin: 0 auto;
		padding: 0 2rem;
	}
	.faq-head {
		font-family: var(--serif);
		font-weight: 400;
		font-size: clamp(1.8rem, 3vw, 2.4rem);
		letter-spacing: -0.02em;
		margin: 1rem 0 3rem;
	}
	.faq-item {
		border-top: 1px solid var(--rule);
		padding: 1.5rem 0;
	}
	.faq-item:last-of-type {
		border-bottom: 1px solid var(--rule);
	}
	.faq-q {
		font-family: var(--serif);
		font-weight: 500;
		font-size: 1.15rem;
		letter-spacing: -0.01em;
		color: var(--ink);
		cursor: pointer;
		list-style: none;
		display: flex;
		justify-content: space-between;
		align-items: center;
	}
	.faq-q::-webkit-details-marker {
		display: none;
	}
	.faq-q::after {
		content: "+";
		color: var(--accent);
		font-size: 1.5rem;
		line-height: 1;
	}
	.faq-item[open] .faq-q::after {
		content: "−";
	}
	.faq-a {
		font-family: var(--serif);
		font-size: 1.02rem;
		line-height: 1.6;
		color: var(--ink-soft);
		margin: 0.8rem 0 0;
		max-width: 60ch;
	}

	/* DOWNLOAD */
	.download {
		padding: var(--section-pad) 0;
		border-top: 1px solid var(--rule-soft);
		background: var(--bg-deep);
		scroll-margin-top: 5rem;
	}
	.download-inner {
		max-width: 980px;
		margin: 0 auto;
		padding: 0 2rem;
	}
	.download-head {
		font-family: var(--serif);
		font-weight: 400;
		font-size: clamp(2rem, 3.5vw, 3rem);
		letter-spacing: -0.025em;
		line-height: 1.1;
		margin: 1rem 0 0.5rem;
	}
	.download-head :global(em) {
		font-style: italic;
		color: var(--accent);
		font-weight: 300;
	}
	.download-dek {
		font-family: var(--serif);
		font-size: 1.1rem;
		color: var(--ink-soft);
		margin: 0 0 3rem;
	}
	.platforms {
		display: grid;
		gap: 1.5rem;
	}
	.platform {
		display: grid;
		grid-template-columns: 1fr;
		gap: 1.5rem;
		padding: 2rem 2.25rem;
		background: var(--bg);
		border: 1px solid var(--rule);
	}
	@media (min-width: 760px) {
		.platform {
			grid-template-columns: 1fr auto;
			align-items: center;
			gap: 2.5rem;
		}
	}
	.platform-head {
		display: flex;
		align-items: center;
		gap: 0.8rem;
		margin-bottom: 1rem;
	}
	.platform-icon {
		font-size: 1.6rem;
	}
	.platform-title {
		font-family: var(--serif);
		font-weight: 500;
		font-size: 1.3rem;
		letter-spacing: -0.015em;
		margin: 0;
	}
	.platform-list {
		list-style: none;
		padding: 0;
		margin: 0;
		display: grid;
		gap: 0.4rem;
		font-family: var(--serif);
		font-size: 0.98rem;
		color: var(--ink-soft);
	}
	.platform-list li {
		padding-left: 1.25rem;
		position: relative;
	}
	.platform-list li::before {
		content: "✓";
		position: absolute;
		left: 0;
		color: var(--accent);
		font-weight: 500;
	}
	.platform-right {
		display: flex;
		flex-direction: column;
		gap: 0.6rem;
	}
	.dl-btn {
		display: inline-flex;
		flex-direction: column;
		align-items: flex-start;
		padding: 0.6rem 1.1rem;
		background: var(--ink);
		color: var(--bg);
		text-decoration: none;
		border-radius: 8px;
		min-width: 180px;
		transition:
			background 0.2s,
			transform 0.2s;
	}
	.dl-btn:hover {
		background: var(--accent);
		transform: translateY(-1px);
	}
	.dl-btn-small {
		font-size: 0.66rem;
		letter-spacing: 0.04em;
		text-transform: uppercase;
		opacity: 0.75;
		line-height: 1.2;
	}
	.dl-btn-main {
		font-size: 0.95rem;
		font-weight: 600;
		line-height: 1.3;
	}

	/* FINALE */
	.finale {
		padding: var(--section-pad) 0;
		border-top: 1px solid var(--rule-soft);
		text-align: center;
	}
	.finale-inner {
		max-width: 820px;
		margin: 0 auto;
		padding: 0 2rem;
	}
	.finale-h {
		font-family: var(--serif);
		font-weight: 400;
		font-size: clamp(2.2rem, 4vw, 3.5rem);
		line-height: 1.1;
		letter-spacing: -0.025em;
		margin: 1.5rem auto 1.5rem;
		max-width: 22ch;
	}
	.finale-h :global(em) {
		font-style: italic;
		color: var(--accent);
		font-weight: 300;
	}
	.finale-p {
		font-family: var(--serif);
		font-size: 1.15rem;
		line-height: 1.5;
		color: var(--ink-soft);
		max-width: 42ch;
		margin: 0 auto 2.25rem;
	}
</style>
